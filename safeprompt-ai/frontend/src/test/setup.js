// Global Vitest setup (loaded via vite.config.js's test.setupFiles).
// Adds jest-dom's DOM-specific matchers (toBeInTheDocument, etc.) to
// every test file automatically.
import '@testing-library/jest-dom'
